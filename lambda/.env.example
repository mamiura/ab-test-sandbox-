# Set to true for local testing without the Datadog Lambda Extension
USE_FALLBACK=true

# Datadog — set these as Lambda environment variables (not in source control)
DD_API_KEY=your-datadog-api-key
DD_SITE=datadoghq.com
DD_ENV=production
DD_SERVICE=ab-test-sandbox-lambda
